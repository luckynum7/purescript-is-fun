{
    "private": true,
    "scripts": {
        "postinstall": "bower install",
        "docs": "pulp docs",
        "test": "pulp test",
        "build": "webpack --progress --bail",
        "dev": "webpack-dev-server --progress --hot --inline",
        "-build:psc": "pulp browserify --jobs 4 --optimise --to dist/app.js",
        "-watch:psc": "pulp --watch browserify --jobs 4 --to dist/app.js"
    },
    "dependencies": {
        "virtual-dom": "^2.1.1"
    },
    "devDependencies": {
        "purescript-psa": "^0.4.0",
        "purs-loader": "^2.2.0",
        "webpack": "^1.14.0",
        "webpack-dev-server": "^1.16.3"
    }
}
