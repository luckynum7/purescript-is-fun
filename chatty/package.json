{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp browserify --jobs 4 --optimise --to dist/app.js",
    "watch": "pulp --watch browserify --jobs 4 --to dist/app.js"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "purescript-psa": "^0.4.0"
  }
}
